# Rollback Netcode Evidence Table

This table is the live implementation companion for
`docs/investigations/rollback-netcode-implementation-plan-2026-07-01.md`.
Update it whenever rollback code copies a field/range, hashes a field, installs
a hook, or excludes a candidate field from the manifest.

Scope note: the current branch validates the same-process rollback core, lab
surface, snapshot lifecycle-refusal helpers, Phase-8 transport/order/session
model, a cache-provenance adapter model, a native live-boundary observability
hook, an idempotent runtime cache-cell write/read/restore probe, a
non-idempotent lab cache-prediction write/read/output-restore probe at the
stock consumer boundary, a pinned GekkoNet stress-session integration self-test,
a deterministic GekkoNet custom-adapter loopback harness, a Horse-owned bridge
envelope that tunnels Gekko payloads through rollback transport metadata, a
Horse-owned localhost UDP Gekko adapter that sends and receives HRG1-wrapped
payloads through real WinSock sockets, a deterministic same-machine
fault-injected two-peer transport harness with named latency/loss/reorder/
duplicate/corruption profiles and ack/resend convergence gates, and a
source/destination/session-bound live-transport queue model that holds
validated HRG1 metadata until the
game-thread stock drain/cache-order boundary, plus an integrated live-peer
pipeline model that proves HRG1/Gekko metadata is not written as stock gameplay
input while decoded confirmed gameplay input remains game-thread/cache-order
owned, and a Gekko gameplay-input bridge that decodes actual `GekkoAdvanceEvent`
`uint32_t` input buffers into that explicit gameplay input contract, plus a
two-peer local end-to-end harness that composes decoded payloads, HRG1 metadata
drain, prediction replacement, and deterministic confirmed-state convergence,
plus a combined live-online-capture readiness gate and guarded live-activation
gate that require direct observe-only stock send/receive readiness fields, live
stock-drain/cache-consumer ordering, Horse adapter route provenance, strict HRG1
route identity values matching the activation request, and explicit operator
arming before any future HRG1/Gekko path can leave observe-only mode. Steam/SC6
live peer packet integration, prediction cache injection in live online matches,
and local input delay removal are intentionally deferred.

Latest rollback validation after same-machine fault-profile integration:
full bundle
`E:\myMods\reports\rollback_validation\rollback_validation_20260703-082341-584926.json`
passed on 2026-07-03, including seventeen standalone self-tests and eleven
request-file in-game lab gates. The bundle rebuild command includes
`RollbackSnapshotSelfTest` and runs
`E:\myMods\build_cmake_LessEqual421__Shipping__Win64\HorseMod\RollbackSnapshotSelfTest.exe`,
closing the stale lower-case snapshot executable gap from low-context review.
It also builds and runs `RollbackFaultInjectSelfTest`, which passed all seven
same-machine transport profiles with `profiles=7/7`, `clean=1`, `wifi=1`,
`bad_wifi=1`, `overseas=1`, `spike=1`, `burst=1`, `corrupt=1`, and
`converged=1`.
`RollbackGekkoUdpSelfTest` reported `enabled=1`, `wsa=1`, `sockets=1`,
`loopback=1`, `nonblocking=1`, `manual=1`, `wrong_endpoint=1`,
`wrong_source=1`, `wrong_dest=1`, `wrong_session=1`, `sent=1`,
`received=1`, `freed=1`, `bidirectional=1`, `bridge=1`, `bridge_meta=1`,
`gameplay_decode=1`, `slots=1`, `state=1`, `checksums=1`, `save=1`,
`load=1`, `advance=1`, `rollback=1`, `no_desync=1`, `packets_sent=20`,
`packets_recv=20`, `bridge_bad=0`, `endpoint_bad=0`, and matching checksums
`0xF0AD2560/0xF0AD2560`; live-online readiness summaries
`live_online_capture_readiness_20260703-082341-584926.json` and
`live_online_capture_trace_readiness_20260703-082341-584926.json` now include
`rollback_live_activation_candidate` evidence. The normal full bundle leaves
the operator arm off and therefore reports `activation_candidate_observed=true`,
`activation_candidate_ok=false`, and `failure=operator-not-armed` while still
requiring `stable_readiness_ok=true`. An armed blank-start smoke run
`live_activation_candidate_remediate_candidate-remediate-20260703-020923.json`
and trace `replay_trace_20260703_020924_pid16112.jsonl` prove the same
candidate then fails closed with `failure=live-traffic-not-proven` while the
human log line carries the matching request id, Horse route provenance, strict
source/destination/session identity, and stock surface rejection gates. Live
peer proof still requires a real online match and the opt-in command
`python tools\rollback_full_validation_run.py --live-online-only --require-live-activation-candidate --live-online-watch-seconds <seconds>`;
and strict replay
`E:\myMods\reports\replay_tests\replay_seek_e2e_20260703-082621-seek.json`
passed on the first attempt with `strict: PASS`, 4/4 watch cases, `2400/2400`
observed frames, and 0 state mismatches.

| Item | Address / Offset | Function / Owner | Type / Field | Evidence | Runtime Test | Restore Source / Exclusion |
|---|---:|---|---|---|---|---|
| Live activation-candidate capture proof | N/A | `Horse::RollbackController`, `EvaluateRollbackLiveActivation`, `tools\rollback_lab_test_run.py`, `tools\rollback_live_online_capture_analyze.py` | `rollback_live_activation_candidate` JSONL/log event beside every `live-online-capture` verdict | Added after the Horse-owned UDP/Gekko adapter so live capture evidence is evaluated through the same final activation gate that will guard any future live HRG1/Gekko route. The request-file path now carries `activation_arm`, source peer, destination peer, and session id; the candidate report requires the captured observe-only stock hooks, boundary hooks, live traffic/order, native session/InputLog bindings, HRG1 payload, Horse adapter provenance, strict route identity, and route values matching the activation request. Low-context P2 remediation added `request_id` to the human candidate log line, filters capture/candidate log lines by current request id, and requires log candidate source/destination/session to equal the requested route values. Follow-up P2 remediation added a shared strict helper so direct `--require-live-activation-candidate` process exits also require an accepted live-online readiness verdict with no later violation/regression. Final P2 remediation made the durable JSONL analyzer route-specific too: `--require-live-activation-candidate` now checks activation source/destination/session against explicit expected values and the full validation live-only path passes those values to both the lab request and analyzer. | Full validation `E:\myMods\reports\rollback_validation\rollback_validation_20260703-023756.json` passed and wrote readiness summaries with `activation_candidate_observed=true`, `activation_candidate_ok=false`, and `failure=operator-not-armed` for the default unarmed readiness run; durable trace summary records expected route `160/176/0x4C495645414354` and `selected_route_matches=true` for the refused default candidate. Armed blank-start smoke `E:\myMods\reports\rollback_validation\live_activation_candidate_remediate_candidate-remediate-20260703-020923.json`, trace `replay_trace_20260703_020924_pid16112.jsonl`, passed readiness while the candidate failed closed with `failure=live-traffic-not-proven`, `request_id=candidate-remediate-20260703-020923`, `operator=1`, `capture=1`, `hrg1=1`, `provenance=1`, `strict_identity=1`, `horse_route=1`, `stock_reject=0`, and `route_identity=1`. Operator-facing live-only attempt `E:\myMods\reports\rollback_validation\rollback_validation_20260703-024759.json` also failed closed after 120s against the running SC6 process: stable readiness and route match were true, but `live_traffic_ok=false`, `activation_candidate.ready=false`, and missing live evidence was `stock_send=0`, `receive=0`, `drain_consumer=0`, `live_order=0`, `session_ptr=0`, `input_log=0`. Regenerated analyzer summary `live_online_capture_trace_live_20260703-024759_missing_gates.json` now emits machine-readable `missing_live_traffic_gates` and `missing_activation_candidate_gates` for the same trace. | Pure validation gate only. It sends no packets and writes no cache cells. Real online proof must use `python tools\rollback_full_validation_run.py --live-online-only --require-live-activation-candidate --live-online-watch-seconds <seconds>` while SC6 is already in/near an online peer match. |
| Direct battle frame boundary | `0x1402DBC60` | `LuxBattle_PerFrameTick` | `FLuxBattlePerFrameTickArgs *pArgs` | Ghidra MCP confirmed `FLuxBattlePerFrameTickArgs` size `24`; `LuxBattleChara_VTableThunk_PerFrameTick @ 0x1403D2A20` tail-jumps with the same ABI. | Final matrix trace `replay_trace_20260702_072651_pid31612.jsonl` has 10 `rollback_resim_window` events for windows `1,2,8,15,60`; every event has `ok=true`, `frame_counter_delta_ok=true`, and `all_steps_ok=true`. | Direct rollback resim boundary. Local input delay removal remains out of scope. |
| HgCpuDirect snapshot writer | `0x1403841E0` | `LuxBattle_HgCpuDirect_ExecMoveChangeAndPost` | `FLuxHgCpuBuffer *pSnapshotBuffer` | Ghidra MCP decompile shows P1/P2 chara streams, globals, camera/timer, motion/physics, terrain, and VFX written through the `FLuxHgCpuBuffer` vtable. | Final matrix trace `072651`: every baseline event has `baseline_ok=true`; every delayed-input event has `predicted_ok=true` and `corrected_ok=true`. | Native rollback snapshot source for broad battle state, plus explicit extras below. |
| HgCpuDirect snapshot restore | `0x140384540` | `LuxBattle_HgCpuDirect_ExecFinalizeAndPost` | `FLuxHgCpuBuffer *pSnapshotBuffer` | Ghidra MCP decompile shows P1/P2 chara stream restore, opponent relink, and subsystem restore. Some native-owned pointer/VFX/cache ranges are rebuilt or canonicalized by the reader. | Final matrix trace `072651`: all events have `restore_start_after_ok=true`, `corrected_matches_baseline=true`, `hgcpu_policy_match=true`, `hgcpu_unignored_mismatch_count=0`, `hgcpu_motion_bank_control_match=true`, and `hgcpu_motion_tail_match=true`. | Native restore source; byte policy excludes only documented native-canonicalized or presentation-cache ranges. |
| BM catch-up loop | `0x1403FE520` | `LuxBattleManager_Tick_SimulationLoop_UpdateInputAndRoundState` | `ALuxBattleManager_Partial *pBattleManager` | Ghidra MCP `batch_analyze_completeness` on 2026-07-01 reported effective score `100`. | Not implemented yet. | Candidate full battle-frame step or cache-path fallback. |
| Chara round-cache consumer | `0x1403FCD10` | `LuxBattleChara_UpdatePlayerInputData_FromRoundCache` | BM/cache consumer | Ghidra MCP typed `pMoveProvider` as `void *`, replaced the plate comment with Algorithm/Parameters/Returns/Special Cases/Structure Layout sections, and documents this as the stock cache read boundary. Effective score on 2026-07-02: `91.13147192765459`; fixable deductions below 10. A PRE comment now records the lab-only cache-injection/cache-prediction probes at this boundary. | Latest cache-prediction trace `replay_trace_20260702_120952_pid27504.jsonl` emitted `rollback_cache_prediction ok=true` after a non-idempotent game-thread cache-cell write, stock consumer observation, and cache/current-input/output-pair restore. Latest cache ownership trace `replay_trace_20260702_121835_pid83588.jsonl` proves direct rollback resim still leaves stock InputLog/cache unchanged while correction succeeds. | Cache-path read boundary. Future live adapter must prove drain/prediction/read ordering and live peer packet integration before relying on this consumer in online matches. |
| Input cache reader | `0x1403F0720` | `GetCachedInputForFrameInputLogSlot` | `ALuxBattleFrameInputLog+0x3C0`, `FLuxReplayInputCacheEntry[2][512]` | Ghidra MCP plate/prototype/variable cleanup typed `ALuxBattleFrameInputLog *pInputLog`, `uint dwPlayerIdx`, `int nFrameID`, and `uint dwFrameIndex`; added EOL comments for `+0x658`, `0x1FF`, entry scale, cache base, and input-value field. The 2026-07-02 plate update records that `RollbackGekkoGameplayInputBridge` decodes `GekkoAdvanceEvent` input bytes as `uint32_t` gameplay values because this reader returns the cache entry's `uint dwInputValue` at `+0x08`; HRG1 metadata `local_input` remains a 64-bit payload hash and must not be consumed here. Effective score on 2026-07-02: `100.0` with no fixable deductions. | Post-hardening cache trace `replay_trace_20260702_095928_pid45912.jsonl` has `rollback_cache_ownership ok=true`, `same_input_log=true`, and `cache_hash_match=true`. | Cache-path tests must still prove drain/prediction/read order before consuming this reader for real online rollback. |
| Input cache local writer | `0x1403F2AB0` | `UpdateFrameInputLogCacheLocalMode` | `ALuxBattleFrameInputLog+0x398/+0x39C/+0x3A0/+0x3A4/+0x3B8/+0x3C0` | Ghidra MCP verified `pInputLog`, `dwSlotBit`, `dwMasterClock`, `dwCurrentInput`, `pReplayInputCacheEntry`, `0x1FF` rolling index, and `0x2000` per-player slab stride; completeness improved to `90.07`. | Final cache traces have `full_hash_match=true`, `cache_hash_match=true`, `master_clock_match=true`, and `drain_cursor_match=true`. | Observed only as a read-only ownership proof in part one. Rollback does not write stock InputLog cache entries yet. |
| Current-input mirror refresh | `0x1403FDF30` | `ProcessFrameInputLogCurrentInputRefresh` | `ALuxBattleFrameInputLog+0x3B8/+0x3BC` | Ghidra MCP verified `ALuxBattleFrameInputLog *pInputLog`, two-slot mirror, active-slot mask `+0x39C`, and active-slot count `+0x398`; completeness improved to `92.0`. | Final cache traces have `current_input_match=true`; P1/P2 current-input values are unchanged before/after direct local resim. | Direct rollback resim leaves the stock current-input mirror unchanged. |
| Online receive enqueue boundary | `0x1403F4BE0` | `LuxOnline_PushToRingBuffer_WithCriticalSection` | `ALuxBattleFrameInputLog` receive deque, `FReplayNetPacketWrapper_Partial *pPacketWrapper`, and `LuxDequeHeapBlock_PacketWrapperPartial` | Ghidra MCP retagged the delegate flag as `byte bUnusedFlag`, typed `pPacketWrapper` and `pOldestPacketCursor` as `FReplayNetPacketWrapper_Partial *`, created the partial deque heap-block struct for the overflow path (`pEntries`, `qwMask`), renamed deque locals, added PRE/EOL comments for the receive lock, queue push, overflow drop, wrapper read state/cursor, and saved the program. A 2026-07-02 PRE comment records that the Horse/Gekko bridge remains offline-only and this live receive-side path must stay enqueue/copy-only. Effective score on 2026-07-02 after the deque-block struct cleanup: `100.0` with no fixable deductions. | Adapter tests below prove Horse can own GekkoNet send/receive/free callbacks without writing stock InputLog cache from this receive-side path. The live-online-capture lab also keeps this receive boundary observe-only while gating future live capture on nonzero receive-enqueue traffic. | Network-thread boundary. Horse/Gekko integration must enqueue or copy packet bytes only here; live cache mutation remains game-thread-owned. |
| Online drain/cache writer | `0x1403F6770` | `LuxOnline_DrainRingBuffer_DecodeInputPackets_AndUpdateCache` | `ALuxBattleFrameInputLog` deque/cache mutation | Ghidra MCP confirms `ALuxBattleFrameInputLog *pFrameInputLog` plus typed packet/deque/cache locals. The rollback plate note records that stock opcode `0`/`1` packets carry only low frame tags and are insufficient for rollback identity; a rollback adapter needs absolute frame ids and confirmations. Completeness effective score on 2026-07-02: `93.0657359638273`. The refreshed 2026-07-02 plate note records the live-transport queue rule: network-side HRG1 decode queues validated metadata only, and cache mutation stays behind this game-thread stock-drain boundary unless a guarded drain-bypass policy is selected. | `tools\rollback_transport_selftest.cpp` proves the model rejects network-thread live-cache writes. Latest boundary trace `replay_trace_20260702_121603_pid77044.jsonl` proves the detour installs and the stock drain is inert during offline replay (`drain_enter=0`, `consumer=6`, `offline_boundary=true`). Latest live-transport trace `replay_trace_20260702_142036_pid82452.jsonl` proves the model queue requires stock-drain completion before delivery and rejects wrong source/session identity before metadata reaches the online-session model. | Future live integration must run stock drain before prediction injection or bypass it under a guarded adapter. Network thread must not write live cache. Real online peer traffic still must prove `live_order=true`. |
| Rollback live-boundary hook | `0x1403F6770`, `0x1403FCD10` | `Horse::RollbackLiveBoundaryHook`, `RollbackLabCase::OnlineBoundaryTrace` | x64Detour on stock drain entry/exit and round-cache consumer entry | Implemented after Ghidra confirmed stock drain and cache-consumer boundaries. The tracker model records drain enter/exit, consumer entry, thread ids, InputLog/BM pointers, master clock, frames-back, and consumed cache frame. It fails on consumer-during-drain, unbalanced drains, missing hooks, missing consumer, or no offline/live ordering proof. The same detour owns the lab-only cache-injection and cache-prediction probes below. | MSVC self-test passed: `rollback live-boundary self-test passed offline=1 live_order=1 bad_consumer_during_drain=1 bad_unbalanced=1`. Strict replay boundary run `E:\myMods\reports\replay_tests\replay_seek_e2e_20260702-121600-seek.json`, trace `replay_trace_20260702_121603_pid77044.jsonl`, passed with `rollback_live_boundary ok=true`, `hooks_installed=true`, `consumer_count=6`, `stock_drain_inert=true`, `offline_boundary_observed=true`, and no drain/consumer ordering violation. | Observability plus lab-only cache probes. It does not send/receive Horse rollback packets. A live online peer test must still prove stock drain exits before prediction/cache consumption or select a guarded drain-bypass path. |
| Rollback input-cache provenance model | N/A | `Horse::RollbackInputCacheShadow`, `RunRollbackInputCacheAdapterSelfTest` | `FLuxReplayInputCacheEntry_Model[2][512]` plus per-cell source metadata | Added after Ghidra confirmed that stock receive only enqueues packet wrappers, stock drain writes cache on the game thread, and the cache consumer reads without taking the deque lock. The model requires game-thread ownership, requires stock drain completion before prediction unless a guarded bypass is selected, rejects network-thread cache writes, separately rejects non-network off-game-thread writes, rejects stock drain after prediction for the same frame, rejects prediction over confirmed cache, replaces predicted cells only with confirmed remote input, and fails stale ring-cell identity mismatches. A PRE comment was added to `LuxOnline_PushToRingBuffer_WithCriticalSection @ 0x1403F4BE0` documenting that Horse rollback must not write `ALuxBattleFrameInputLog+0x3C0` from the receive-side path; Ghidra program saved. | MSVC self-test passed after the bridge update: `rollback input-cache adapter self-test passed layout=1 pred_after_drain=1 net_reject=1 not_game_thread=1 drain_required=1 stock_after_pred=1 confirmed_replace=1 dup_confirmed=1 pred_over_confirmed=1 conflict=1 consume_source=1 ring_mismatch=1`. The online-session self-test now also requires `cache_provenance=1`. | Pure provenance/ordering model only. It does not write live cache cells and does not yet prove real online peer traffic. |
| Rollback cache-injection runtime probe | `0x1403FCD10`, `BM+0x478`, `InputLog+0x3C0`, `BM+0x1498` | `Horse::RollbackLiveBoundaryHook`, `RollbackLabCase::CacheInjectionTrace` | guarded idempotent game-thread cache-cell write/read/restore at the stock round-cache consumer | Added after Ghidra re-confirmed that `LuxBattleChara_UpdatePlayerInputData_FromRoundCache` reads `GetCachedInputForFrameInputLogSlot(pInputLog, player, pInputLog->nLastFrameId, (pInputLog->nMasterClock - nFramesBack) - 1)` and then writes the current-input arrays at `BM+0x1498`. The hook computes the same cache cell from `BM+0x478`, `InputLog+0x3A4`, and `framesBack`, only proceeds when the source cell identity matches, performs an idempotent `SafeWriteBytes`, checks that the stock consumer observed the cell through `BM+0x1498`, and restores the cache cell. | Latest strict replay cache-injection run `E:\myMods\reports\replay_tests\replay_seek_e2e_20260702-121347-seek.json`, trace `replay_trace_20260702_121350_pid58800.jsonl`, passed with `rollback_cache_injection ok=true`, `hooks_installed=true`, `attempted=true`, `context_ready=true`, `source_cell_valid=true`, `wrote_cache=true`, `consumer_observed_cache=true`, `restored_cache=true`, and `idempotent_write=true`. | Lab-only mechanical proof that Horse can touch the live cache cell at the consumer boundary and observe the stock read path. The stronger non-idempotent proof is the cache-prediction row below. |
| Rollback cache-prediction runtime probe | `0x1403FCD10`, `BM+0x478`, `InputLog+0x3C0`, `BM+0x1498`, `BM+0x14A8` | `Horse::RollbackLiveBoundaryHook`, `RollbackLabCase::CachePredictionTrace` | guarded non-idempotent game-thread prediction write, stock consumer observation, and output restore | Added after the idempotent probe. The hook waits for an unsuppressed consumer call, computes the same source cache cell, flips one input bit, writes the altered entry, lets the stock consumer run, verifies `BM+0x1498` current input and `BM+0x14A8` held/edge pair observed the predicted value, then restores the cache entry, current-input slot, and input-pair slot to the stock-original result before the detour returns. Ghidra PRE comment at `0x1403FCD10` now documents this stronger probe; program saved. | Strict replay cache-prediction run `E:\myMods\reports\replay_tests\replay_seek_e2e_20260702-120948-seek.json`, trace `replay_trace_20260702_120952_pid27504.jsonl`, passed with `rollback_cache_prediction ok=true`, `non_idempotent_write=true`, `injected_differs_from_original=true`, `original_input=0`, `injected_input=1`, `observed_current_input=1`, `restored_current_input_value=0`, `observed_output_input=1`, `observed_output_flags=1`, `restored_output_input=0`, `restored_output_flags=0`, `cache_entry=0x1B7D0DC03D0`, `current_input_slot=0x1B7D84121F0`, and `input_pair_slot=0x1B8E2762BC0`. Replay strict analyzer also passed with 4/4 cases, `2400/2400` observed frames, and 0 state mismatches. | Lab-only proof that a non-idempotent prediction value can be observed by the stock consumer and fully restored during replay. It is not yet wired to live peer packets, stock online drain ordering, or a real online match. |
| Stock online input senders | `0x1403F84E0`, `0x1403F8710` | `LuxOnline_SendInputPacket_PerFrame_Opcode0`, `LuxOnline_SendInputPacket_BatchedRange_Opcode1` | Stock opcode 0 single-frame input and opcode 1 resend/cache packet | Ghidra MCP typed/renamed the remaining parameter/serializer/shared-ref locals, added required plate sections and EOL/PRE comments, and saved. `LuxOnline_SendInputPacket_PerFrame_Opcode0` plate notes now record that HRG1 v2 carries source peer, destination peer, and a 64-bit session id in a separate Horse adapter envelope, not in stock opcode 0. Opcode 0 verifies at effective `93.0657359638273`; opcode 1 also previously verified at effective `93.0657359638273`. Plate notes state opcode 0/1 packets use low-frame tags and stock cache semantics, not rollback identity. | `rollback_transport_selftest`, `RollbackGekkoSelfTest`, and `RollbackLiveTransportSelfTest` cover the separate Horse packet, ack, handshake, hash, loopback model, HRG1 v2 source/session binding, and offline Gekko bridge envelope. Replay traces below prove the offline model does not regress same-process rollback/replay behavior. | Do not overload stock opcode 0/1 for rollback metadata. Future live adapter must send absolute-frame Horse packets through a guarded path. |
| Stock online transport surface guard | `0x1403F0CC0`, vtable slots `+0x18/+0x20/+0x28`, channels `5/6/7` | `AcquireLuxOnlineTransportSessionSharedPtr`, `Horse::RollbackStockTransportSurface`, `RollbackLabCase::StockTransportSelfTest` | native transport shared-pointer layout plus stock send-slot/channel taxonomy | Ghidra MCP typed `AcquireLuxOnlineTransportSessionSharedPtr` as returning `TSharedPtrLuxOnlineTransportSession *`, created `TSharedPtrLuxOnlineTransportSession` with `pSession` at `+0` and `pRefController` at `+8`, and documented vtable evidence: slot `+0x18` sends BattleSync replies on channel 6, slot `+0x20` sends stock opcode 0/1 input on channel 5, and slot `+0x28` sends typed BattleSync/request messages. `RollbackStockTransportSurface` codifies this evidence and rejects HRG1 on stock input/BattleSync/high-level KV paths before considering any Horse-owned adapter marker, while allowing HRG1 only on the Horse-owned adapter route with the Horse adapter provenance cookie, actual nonzero source peer, distinct nonzero destination peer, and nonzero session id values. A 2026-07-02 Ghidra plate/PRE refresh at `0x1403F0CC0` records that native stock transport acquisition is not Horse adapter provenance. | Standalone `RollbackStockTransportSelfTest` in full validation `E:\myMods\reports\rollback_validation\rollback_validation_20260702-232726.json` passed with `failure=ok`, `shared_ptr=1`, `slots=1`, `channels=1`, `input_reject=1`, `battle_reject=1`, `kv_reject=1`, `unknown_reject=1`, `provenance=1`, `identity=1`, `identity_values=1`, `horse_allow=1`, `stock_native=1`, `stock_no_hrg1=1`, `flag_override=1`, and `bridge_v2=1`. In-game trace `replay_trace_20260702_233028_pid13252.jsonl` emitted `rollback_stock_transport_selftest ok=true` with `adapter_provenance_required=true`, `strict_identity_values_required=true`, `adapter_flag_cannot_override_stock=true`, and all gates true. Required strict replay regression `E:\myMods\reports\replay_tests\replay_seek_e2e_20260702-233209-seek.json`, trace `replay_trace_20260702_233209_pid84476.jsonl`, passed with `strict: PASS`, 4/4 watch cases, `2400/2400` observed frames, and 0 state mismatches. | Guard/model only. It documents where HRG1 must not be sent yet; it is not a live Steam/SC6 send hook. Future live integration must either prove a safe native send seam or keep using a Horse-owned adapter path. |
| Stock online transport observe hook | `0x1403F0CC0`, `0x1403F84E0`, `0x1403F8710`, `0x14051DBC0`, `0x1403F4BE0` | `Horse::RollbackStockTransportObserveHook`, `RollbackLabCase::StockTransportObserve` | observe-only x64Detours on native transport acquisition, stock opcode 0 send, stock opcode 1 send, BattleSync RequestStage send, and receive-side packet enqueue | Added after Ghidra confirmed the native send taxonomy and receive enqueue boundary. The hook records counters and last-seen pointers/frames/channels for the acquisition helper, stock input opcode 0/1 senders, BattleSync RequestStage, and `LuxOnline_PushToRingBuffer_WithCriticalSection`, then immediately calls the original. It does not modify payload bytes, channels, vtables, shared pointers, routing, packet wrappers, or cache cells. Ghidra plate comment at `0x1403F4BE0` documents the observe-only receive probe and the `--require-stock-observe-receive` live-capture gate; after adding `LuxDequeHeapBlock_PacketWrapperPartial`, completeness verifies at effective `100.0`. Ghidra cleanup at `LuxOnlineBattleSync_RequestStage_SendOpcode6 @ 0x14051DBC0` records stock BattleSync slot `+0x28`, channel `6`, and message type `2` as liveness proof only, not an HRG1 send path; effective score after comment cleanup is `90.74086836023766`. | Standalone `RollbackStockTransportObserveSelfTest` passed with `hooks=1`, `trace=1`, `acquire=1`, `opcode0=1`, `opcode1=1`, `battle=1`, `recv=1`, `totals=1`, `observed=5`. Full validation bundle `E:\myMods\reports\rollback_validation\rollback_validation_20260702-232726.json` reran the blank-start stock-observe lab; trace `replay_trace_20260702_233055_pid19816.jsonl` emitted `rollback_stock_transport_observe ok=true`, `case=stock-observe`, `request_id=20260702-232726`, `observe_only=true`, `hooks_installed=true`, `trace_active=true`, `receive_enqueue_hook_installed=true`, all five hook gates true, and `total_observed_calls=0`. Required strict replay regression from the same bundle `E:\myMods\reports\replay_tests\replay_seek_e2e_20260702-233209-seek.json`, trace `replay_trace_20260702_233209_pid84476.jsonl`, passed with `strict: PASS`, 4/4 watch cases, `2400/2400` observed frames, and 0 state mismatches. | Observe-only live seam. Blank boot proves hook installation, not live traffic. A real online match still must produce nonzero acquire/opcode/BattleSync/receive-enqueue counts before any HRG1 injection is attempted. |
| Combined live-online capture gate | `0x1403F0CC0`, `0x1403F84E0`, `0x1403F8710`, `0x14051DBC0`, `0x1403F4BE0`, `0x1403F6770`, `0x1403FCD10` | `Horse::RollbackLiveOnlineCapture`, `RollbackLabCase::LiveOnlineCapture` | observe-only stock transport counters plus live stock-drain/cache-consumer ordering report | Added as the real-online readiness gate before any guarded HRG1 injection. It combines the stock transport observe report with the live-boundary report and requires observe-only mode, stock send/receive hooks installed and active, boundary hooks installed and active, no boundary violation, nonnull session observation, stock input send, BattleSync send, receive enqueue, drain enter/exit, consumer observation, and `live_order_proven=true` before `live_capture_complete=true`. The default readiness lab passes with `capture_ready=true` while deliberately reporting `failure=waiting-for-live-online-traffic` when no real online match traffic exists; verdict emission is delayed until the native image base is ready so early startup does not look like a boundary-hook failure. | Standalone `RollbackLiveOnlineCaptureSelfTest` passed with `ready=1`, `live=1`, `recv=1`, `violation_case=1`, `acquire=1`, `input=1`, `battle=1`, `receive=1`, `drain=1/1`, `consumer=1`, `total=7`. Full validation bundle `E:\myMods\reports\rollback_validation\rollback_validation_20260702-232726.json` ran the request-file in-game `live-online-capture` lab; trace `replay_trace_20260702_233123_pid82828.jsonl` emitted image-base-ready `rollback_live_online_capture ok=true`, `request_id=20260702-232726`, `capture_ready=true`, `live_capture_complete=false`, `observe_only=true`, `stock_hooks_installed=true`, `stock_trace_active=true`, `boundary_hooks_installed=true`, `boundary_trace_active=true`, `boundary_violation=false`, and `failure=waiting-for-live-online-traffic`. The bundle wrote both `E:\myMods\reports\rollback_validation\live_online_capture_readiness_20260702-232726.json` and `E:\myMods\reports\rollback_validation\live_online_capture_trace_readiness_20260702-232726.json`; the analyzer saw 4 capture events with `readiness_ok=true`, `stable_readiness_ok=true`, `live_traffic_ok=false`, `stable_live_traffic_ok=false`, `boundary_violation_seen=false`, `readiness_regression_seen=false`, `bad_event_count=0`, `selected_source=readiness`, `require_case=live-online-capture`, `require_request_id=20260702-232726`, `filtered_request_count=0`, and `filtered_event_count=0`. The operator-facing live-match command is `python tools\rollback_full_validation_run.py --live-online-only --require-live-activation-candidate --live-online-watch-seconds <seconds>` after the branch has been deployed and SC6 is already staged in/near an online peer match. | Safety gate only. It proves the instrumentation is armed without modifying stock traffic; it does not claim live rollback until real online traffic produces nonzero send/receive/drain/consumer counts and live ordering. |
| Live rollback activation gate | `0x14051DBC0` plus the live-capture and stock-route surfaces above | `Horse::RollbackLiveActivationGate`, `RollbackLabCase::LiveActivationSelfTest` | final pure policy gate before any future HRG1/Gekko live path can leave observe-only mode | Added after the readiness-only local pipeline was validated, so a future live path has a single explicit gate rather than scattered boolean checks. `EvaluateRollbackLiveActivation` requires explicit operator arming, direct capture readiness (`observe_only`, stock observe hooks/trace, and boundary hooks/trace), `live_capture_complete=true`, no boundary violation, stock input and BattleSync liveness, receive enqueue, stock drain enter/exit plus consumer observation, `live_order_proven=true`, nonzero native session pointer, both send/input-log and receive/input-log pointers, HRG1 payload, Horse adapter route provenance, strict route identity, `AllowHorseOwnedHrg1`, route source/destination/session values matching the activation request, distinct nonzero peer ids, and a nonzero 64-bit session id. Ghidra notes document BattleSync RequestStage as stock liveness evidence only and `AcquireLuxOnlineTransportSessionSharedPtr @ 0x1403F0CC0` as native stock acquisition, not Horse adapter provenance. | Standalone `RollbackLiveActivationSelfTest` in full validation `E:\myMods\reports\rollback_validation\rollback_validation_20260702-232726.json` passed with `ready=1`, `readiness_only=1`, `stock=1`, `identity=1`, `boundary=1`, `session=1`, `input_log=1`, `self_peer=1`, `zero_session=1`, `operator=1`, `receive=1`, `non_hrg1=1`, `route_provenance=1`, `direct_ready=1`, and `route_identity=1`. In-game request-file lab `lab-live-activation` from the same bundle wrote trace `replay_trace_20260702_233002_pid51932.jsonl`; the image-base-ready event emitted `rollback_live_activation_selftest ok=true`, `request_id=20260702-232726`, `activation_ready=true`, `route_provenance_rejected=true`, `direct_readiness_rejected=true`, `route_identity_rejected=true`, and all refusal gates true. | Pure activation policy only. It sends no packets and writes no cache cells. A real online match must still satisfy the live traffic/order capture and then route only through the Horse-owned HRG1/Gekko path before activation is allowed. |
| Activation-gated live HRG1 executor | `0x1403F4BE0`, `0x1403F84E0`, stock drain/cache-consumer boundaries above | `Horse::RollbackLiveActivationExecutor`, `RollbackLabCase::LiveActivationExecutorSelfTest` | activation-owned HRG1 queue, metadata drain, decoded gameplay prediction/confirmation/consume path | Added after Ghidra PRE comments on the receive enqueue and stock opcode 0 sender clarified that the executor is Horse-owned only: stock receive enqueue remains packet-wrapper enqueue/copy only, stock opcode 0 remains low-frame SC6 input traffic, and no HRG1/cache path is allowed without adapter provenance plus strict source/destination/session identity from `EvaluateRollbackLiveActivation`. The executor refuses every enqueue/drain/cache operation unless `activation_ready=true`, then enqueues exactly one Horse-provenance HRG1 route, requires stock drain before metadata acceptance, keeps metadata `local_input` separate from decoded gameplay input, applies prediction/confirmation only on the cache-order path, and rejects network-thread cache writes plus wrong source/destination/session packets. Decoded gameplay input now carries source/destination/session provenance too, so cache prediction/confirmation/consume rejects a route mismatch even after activation is armed. | Full validation `E:\myMods\reports\rollback_validation\rollback_validation_20260703-012415.json` passed standalone `RollbackLiveActivationExecutorSelfTest` and in-game `lab-live-activation-executor`. The standalone report had `activation_required=1`, `readiness_only=1`, `stock=1`, `provenance=1`, `route_identity=1`, `ready=1`, `enqueue=1`, `queued_only=1`, `stock_drain=1`, `metadata=1`, `metadata_not_gameplay=1`, `predict=1`, `apply=1`, `consume=1`, `net_cache_reject=1`, `wrong_source=1`, `wrong_dest=1`, `wrong_session=1`, `decoded_route=1`, `enqueued=1`, `drained=1`, `rejected=3`, and `cache_writes=2`; request-file trace `replay_trace_20260703_012522_pid88324.jsonl` emitted `wrong_destination_rejected=true`, `decoded_route_rejected=true`, `rejected_packets=3`, and `cache_write_sequence=2`. | Model/lab executor only. It does not inject into Steam/SC6 live packet I/O, does not bypass stock online transport, and remains blocked until a real online match proves live traffic/order and operator activation. |
| Live-online attach isolation | N/A | `tools\rollback_full_validation_run.py`, `tools\rollback_lab_test_run.py`, `tools\rollback_live_online_capture_analyze.py`, `Horse::RollbackLab` | Per-run `request_id` propagated through atomic request file, command-line fallback, validation report, live summary, `rollback_stock_transport_observe`, and `rollback_live_online_capture` ReplayTrace events | Added after low-context review found stale same-case trace, one-shot request consumption, and non-atomic live attach request risks. The full/live-only orchestrators pass the validation `run_id` as `request_id`; the request writer uses same-directory temp-file + flush/fsync + atomic replace; the C++ reader retries transient read failures; the durable analyzer filters by both `case=live-online-capture` and `request_id`; live-online verdicts wait for native image-base readiness before reporting boundary-hook readiness. After the final low-context parser review, `rollback_lab_test_run.py` now parses exact whitespace-delimited `field=value` tokens so strict gates cannot pass through aliases such as `session=1` inside `zero_session=1`. | No-game live-only smoke `reports\rollback_validation\live_online_only_probe_atomic2\rollback_validation_20260702-195800.json` failed closed with `live-traffic-not-proven`, `request_id=20260702-195800`, missing `trace_file=`, and no stale request/temp file. Full validation `reports\rollback_validation\rollback_validation_20260702-232726.json` passed; analyzer artifact `live_online_capture_trace_readiness_20260702-232726.json` selected an image-base-ready event with `request_id=20260702-232726`, `require_request_id=20260702-232726`, `filtered_request_count=0`, and `filtered_event_count=0`. | Validation isolation only. It prevents stale trace acceptance and supports same-process retries; it still does not prove real live rollback until an online match produces nonzero send/receive/drain/consumer counts and `live_order_proven=true`. |
| Rollback transport packet/model | N/A | `Horse::RollbackTransportPacket`, `EncodeRollbackTransportPacket`, `DecodeRollbackTransportPacket`, `RollbackTransportPeerModel`, `ValidateRollbackHandshake`, `CheckRollbackStateHash` | 48-byte little-endian packet with magic/version, absolute frame id, last confirmed remote frame, input, optional state hash, resend base/count, prediction age, and rollback depth | Implemented as Phase-8 groundwork after Ghidra proved the stock online packet path cannot carry rollback identity safely. The model accepts out-of-order absolute frames, advances contiguous confirmed history, tracks duplicate/conflict/reorder/late metrics, refuses over-window late input, treats `kRollbackTransportNoFrame` as an unknown/no-ack sentinel, rejects incompatible or invalid peer handshakes, validates hash-policy enum values, and separates warn-only from enforced state-hash mismatches. | Post-review MSVC self-test passed: `rollback transport self-test passed contiguous=2 accepted=3 duplicates=2 reordered=1 conflicts=2 over_window=1 max_prediction_age=1 max_rollback_depth=1 late_dup=1 late_conflict=1 ack_monotonic=1 invalid_packets=1 handshake=1 handshake_reject=1 handshake_invalid=1 hash_policy=1 loopback=1`. | Model and packet/session contract only. It is not a live Steam/SC6 transport hook and does not yet feed online matches. |
| Same-machine fault-injected transport profiles | N/A | `Horse::RollbackFaultInjectedChannel`, `RollbackFaultAcceptedInputTracker`, `RunRollbackFaultProfileSimulation`, `RollbackFaultInjectSelfTest`, `tools\rollback_full_validation_run.py --profile` | Two deterministic local peers exchanging HRB1 rollback packets through seeded network profiles | Added for online rollback testability without bypassing Steam or pretending one Steam identity is two peers. Profiles cover clean loopback, Wi-Fi jitter, bad Wi-Fi with 120 ms and 5% loss, overseas 180 ms with 2% loss, 10-second latency spikes, 500 ms burst loss, and corrupt-packet rejection. The harness sends new frames plus bounded ack-based resends until both peers confirm the same absolute-frame window, then hashes each peer's actually accepted per-frame payload history in frame order and requires both checksums to match the expected deterministic two-peer checksum. Loss/corruption/burst profiles must observe at least one first-send fault later accepted from a resend. It also hardens the transport model so already-confirmed identical over-window retransmits are classified as `Duplicate`, changed retransmits as `Conflict`, and genuinely new late packets as `OverWindowLate`. Ghidra plate notes at `AcquireLuxOnlineTransportSessionSharedPtr @ 0x1403F0CC0` record that these profiles are Horse-owned HRB1/HRG1 model tests and not stock Lux/Steam transport injection. | Standalone `RollbackFaultInjectSelfTest` passed `profiles=7/7`, `clean=1`, `wifi=1`, `bad_wifi=1`, `overseas=1`, `spike=1`, `burst=1`, `corrupt=1`, `converged=1`. Targeted `corrupt_probe` output showed accepted payload proof and causal resend recovery: `accepted_a=120`, `accepted_b=120`, `payload_match=1`, `first_faults_ab=6`, `first_faults_ba=9`, `resend_recovered_ab=6`, `resend_recovered_ba=9`, and matching `expected/checksum_a/checksum_b=0xE5977A35`. Targeted profile validation passed through the bundle runner in `E:\myMods\reports\rollback_validation\rollback_validation_20260703-082337-047586.json` with `--profile corrupt_probe`; the post-remediation full bundle `E:\myMods\reports\rollback_validation\rollback_validation_20260703-082341-584926.json` also passed all profiles. | Same-machine transport model only. It proves deterministic fault injection, accepted-payload convergence, resend recovery after first-send loss/corruption/burst faults, and report generation, but does not use Steam, SC6 live matchmaking, stock Lux transport packet injection, or local input-delay removal. |
| Rollback online-session adapter model | N/A | `Horse::RollbackOnlineSessionModel`, `RunRollbackOnlineSessionSelfTest` | Packet-to-correction session model over `RollbackTransportPacket` and `RollbackInputHistory` | Builds local absolute-frame packets with peer confirmation, predicts remote input from the newest confirmed remote frame strictly before the prediction target, accepts delayed/out-of-order confirmed Horse packets, reports correction start/depth, rejects duplicate/conflict/over-window late packets, enforces state-hash policy, and calls cache-order validation before accepting packets. The separate cache-provenance self-test is required by the online-session self-test gate, but it is not yet wired into a live session accept/write path. | Latest MSVC self-test passed: `rollback online-session self-test passed ack=1 predict=1 no_correction=1 correction=1 reorder=1 duplicate=1 conflict=1 late=1 reorder_seed=1 no_future_seed=1 cache_write=1 stock_drain=1 bypass=1 cache_provenance=1 hash_enforced=1 hash_warn=1`. Latest in-game online-session trace `replay_trace_20260702_122528_pid45744.jsonl` emitted `rollback_online_session_selftest ok=true` with `cache_provenance_ok=true`. | Adapter/session model only. It is not yet wired to SC6/Steam transport or GekkoNet. |
| GekkoNet session core and adapter harness | N/A | `Horse::RollbackGekkoSessionSelfTest`, `Horse::RollbackGekkoAdapterSelfTest`, `RollbackGekkoSelfTest`, `RollbackLabCase::GekkoSessionSelfTest`, `RollbackLabCase::GekkoAdapterSelfTest` | Pinned GekkoNet no-ASIO static dependency, stress-session event adapter, and deterministic delayed custom-adapter loopback | GekkoNet is fetched from `https://github.com/HeatXD/GekkoNet.git` at commit `02c447c9d6a8a478070b2c162d15370dfad34482` with `NO_ASIO_BUILD=ON`, `BUILD_SHARED_LIBS=OFF`, and `HORSE_ENABLE_GEKKONET=1`. Top-level CMake now verifies the cached `gekkonet-src` HEAD before enabling `FETCHCONTENT_FULLY_DISCONNECTED`, avoiding stale-source bypass of the pin. The public API exposes custom `GekkoNetAdapter` send/receive/free hooks and save/load/advance/desync events. The Horse loopback adapter uses fixed-capacity delayed queues plus malloc/free ownership matching GekkoNet's `receive_data` cleanup contract. | Temp no-ASIO static build passed. Latest standalone `RollbackGekkoSelfTest` passed both the stress-session and adapter gates: session checksum `0x1004AFC6`, `checksum_expected=1`, adapter bidirectional callbacks true, `adapter_load=1`, `adapter_rollback=1`, `adapter_packets_sent=345`, `adapter_packets_recv=339`, `adapter_frees=1017`, and adapter checksums `0x68FC89EA/0x68FC89EA`. In-game traces `replay_trace_20260702_131033_pid10120.jsonl` and `replay_trace_20260702_134714_pid46804.jsonl` emitted `rollback_gekko_session_selftest ok=true` with `final_checksum_expected=true` and `rollback_gekko_adapter_selftest ok=true` with load/rollback-advance/no-desync/send/receive/free/bidirectional/checksum/destroy gates passing. | Session core plus deterministic delayed queued adapter only. It is not yet wired to live Steam/SC6 peer packets, stock drain ordering, or live online-match rollback. |
| Horse/Gekko transport bridge envelope | N/A | `Horse::RollbackGekkoTransportBridge`, `Horse::RollbackGekkoAdapterSelfTest` | `RollbackGekkoBridgeWirePacket` (`HRG1` v2 magic/version, source/destination peers, 64-bit session id, sequence, payload length/hash, embedded `RollbackTransportPacket`, payload bytes) | Added after Ghidra confirmed stock opcode 0/1 packets cannot safely carry rollback identity. The bridge wraps arbitrary GekkoNet adapter payloads in a Horse-owned envelope, embeds the existing absolute-frame rollback metadata, validates magic/version/header/total length/destination/source/session/payload hash/metadata frame on decode, requires embedded metadata `local_input` and derived `state_hash` to match the HRG1 payload hash, and rejects malformed, metadata-mismatched, null, empty, oversized, wrong-source, wrong-destination, or wrong-session packets. The adapter accepts decoded metadata through `RollbackTransportPeerModel<512>` before exposing only the unwrapped bytes back to GekkoNet. The two-peer adapter loopback now also decodes every `GekkoAdvanceEvent` input buffer through `RollbackGekkoGameplayInputBridge` and advances deterministic state only from those decoded `uint32_t` gameplay values. | Standalone `RollbackGekkoSelfTest` on 2026-07-02 passed with `bridge=1`, `bridge_meta=1`, `bridge_reject=1`, `bridge_encoded=345`, `bridge_decoded=339`, `bridge_bad=0`, `adapter_gameplay_decode=1`, `adapter_gameplay_slots=1`, `adapter_gameplay_state=1`, `adapter_gameplay_events=87`, `adapter_gameplay_inputs=174`, and matching adapter checksums `0x68FC89EA/0x68FC89EA`. Full validation bundle `E:\myMods\reports\rollback_validation\rollback_validation_20260702-163631.json` reran the in-game `gekko-adapter` lab; trace `replay_trace_20260702_163707_pid87716.jsonl` emitted `bridge_roundtrip=true`, `bridge_metadata_accepted=true`, `bridge_rejections_ok=true`, `gameplay_decode=true`, `gameplay_slots=true`, `gameplay_state=true`, `gameplay_decoded_events=87`, `gameplay_decoded_inputs=174`, and `bridge_packets_rejected=0`. Required strict replay regression from the same bundle `E:\myMods\reports\replay_tests\replay_seek_e2e_20260702-163852-seek.json`, trace `replay_trace_20260702_163853_pid11348.jsonl`, passed with `strict: PASS`, 4/4 watch cases, `2400/2400` observed frames, and 0 state mismatches. | Offline/same-process bridge contract only. It still uses deterministic loopback queues, not Steam sockets or SC6 live peer packets. |
| Horse/Gekko UDP adapter | N/A | `Horse::RollbackGekkoUdpAdapter`, `RunRollbackGekkoUdpSelfTest`, `RollbackGekkoUdpSelfTest`, `RollbackLabCase::GekkoUdpSelfTest` | WinSock IPv4 loopback sockets carrying HRG1 v2 wrapped GekkoNet custom-adapter payloads | Added after the deterministic Gekko adapter and HRG1 bridge were validated. The adapter starts WinSock, opens nonblocking UDP sockets bound to `127.0.0.1`, performs an immediate manual HRG1 UDP roundtrip, then pumps two `GekkoGameSession` instances through the custom send/receive/free callbacks while a delayed outbound UDP queue forces save/load/rollback-advance coverage. Endpoint decode rejects wrong UDP source endpoint before bridge decode, and rejects wrong HRG1 source peer, destination peer, and session id before any payload reaches GekkoNet; decoded `GekkoAdvanceEvent` buffers still flow through `RollbackGekkoGameplayInputBridge` as `uint32_t` gameplay inputs. A Ghidra PRE comment at `AcquireLuxOnlineTransportSessionSharedPtr @ 0x1403F0CC0` records that this HRG1 route is Horse-owned and separate from stock transport acquisition. | Full validation bundle `E:\myMods\reports\rollback_validation\rollback_validation_20260703-012415.json` built and ran `RollbackGekkoUdpSelfTest`, which passed with `wsa=1`, `sockets=1`, `loopback=1`, `nonblocking=1`, `manual=1`, `wrong_endpoint=1`, `wrong_source=1`, `wrong_dest=1`, `wrong_session=1`, `sent=1`, `received=1`, `freed=1`, `bidirectional=1`, `bridge=1`, `bridge_meta=1`, `gameplay_decode=1`, `slots=1`, `state=1`, `checksums=1`, `save=1`, `load=1`, `advance=1`, `rollback=1`, `no_desync=1`, `frames=48`, `packets_sent=20`, `packets_recv=20`, `bridge_encoded=20`, `bridge_decoded=20`, `bridge_bad=0`, `endpoint_bad=0`, `gameplay_events=5`, `gameplay_inputs=10`, and matching checksums `0xF0AD2560/0xF0AD2560`. The in-game request-file lab `lab-gekko-udp` wrote trace `replay_trace_20260703_012507_pid52308.jsonl` and emitted `rollback_gekko_udp_selftest ok=true` with `wrong_endpoint_rejected=true`, the same route, socket, bridge, gameplay, rollback, and checksum gates true, and normal-session `endpoint_packets_rejected=0`. | Real localhost socket I/O for the Horse-owned adapter route only. It does not inject into Steam/SC6 live packet I/O, does not prove online peer NAT/session behavior, and does not remove local input delay. |
| Rollback live-transport queue | N/A | `Horse::RollbackLiveTransportQueue`, `RunRollbackLiveTransportQueueSelfTest`, `RollbackLabCase::LiveTransportSelfTest` | fixed-capacity HRG1 v2 metadata queue feeding `RollbackOnlineSessionModel` only after source/destination/session/cache-order approval | The queue decodes HRG1 wire bytes, rejects malformed bridge metadata, wrong source, wrong destination, and wrong session id, stores only validated rollback metadata, and does not call `RollbackOnlineSessionModel::receive_remote_packet` from the network enqueue path. `drain_one_to_session` requires `stock_drain_complete=true` unless `RollbackInputCacheMode::DrainBypass` is explicitly selected; duplicate, correction, late/over-window, and queue-capacity behavior are covered. Ghidra plate comments on `LuxOnline_DrainRingBuffer_DecodeInputPackets_AndUpdateCache @ 0x1403F6770` now document this queue and the live-peer pipeline as models, not stock packet replacements. | Standalone `RollbackLiveTransportSelfTest` passed with `enqueue=1`, `bad=1`, `wrong_source=1`, `wrong_dest=1`, `wrong_session=1`, `queued_only=1`, `stock_drain=1`, `drain=1`, `correction=1`, `duplicate=1`, `late=1`, `bypass=1`, `capacity=1`, `enqueued=7`, `drained=5`, `rejected=5`, `queued=2`. In-game lab trace `replay_trace_20260702_142036_pid82452.jsonl` emitted `rollback_live_transport_selftest ok=true` with all policy gates true. | Model/lab bridge only. It validates the network-to-game-thread cache-order and peer/session identity contract, but still does not send or receive Steam/SC6 live peer packets. |
| Rollback live-peer pipeline | N/A | `Horse::RollbackLivePeerPipeline`, `RunRollbackLivePeerPipelineSelfTest`, `RollbackLabCase::LivePeerPipelineSelfTest` | integrated HRG1 metadata queue, online-session accept path, and explicit decoded-gameplay-input cache apply model | Added after the Ghidra drain-boundary refresh. The pipeline proves HRG1 v2 wire bytes are accepted only after source/destination/session/cache-order gates, that the network enqueue path is queued-only, and that the HRG1/Gekko `local_input` payload hash is not written to `ALuxBattleFrameInputLog+0x3C0` as gameplay input. Confirmed gameplay input is represented as a separate decoded input and may update the cache shadow only on the game thread after stock drain or under explicit drain-bypass. Network-thread live-cache attempts now produce the explicit `NetworkThreadCacheWrite` status instead of only a broad not-game-thread rejection. Ghidra plate comment at `0x1403F6770` records this split; effective score remains `93.0657359638273`. | Standalone `RollbackLivePeerPipelineSelfTest` passed with `enqueue=1`, `queued_only=1`, `stock_drain=1`, `metadata=1`, `payload_not_cache=1`, `predict_cache=1`, `confirm_replace=1`, `consume_confirmed=1`, `duplicate=1`, `pred_over_confirmed=1`, `wrong_identity=1`, `late_no_cache=1`, `net_cache_reject=1`, `bypass=1`, `enqueued=2`, `drained=2`, `rejected=3`, and `cache_writes=3`. In-game request-file trace `replay_trace_20260702_154518_pid23460.jsonl` emitted the same gates with `source=request-file`, proving the no-parameter lab launch path. Required strict replay regression after the explicit network-thread status change `E:\myMods\reports\replay_tests\replay_seek_e2e_20260702-154554-seek.json`, trace `replay_trace_20260702_154554_pid86984.jsonl`, passed with `strict: PASS`, 4/4 watch cases, `2400/2400` observed frames, and 0 state mismatches. | Integrated model/lab contract only. It still does not send Steam/SC6 live peer packets, decode real Gekko gameplay input from a live SC6 match, or enable prediction writes in online matches. |
| Gekko gameplay input bridge | N/A | `Horse::RollbackGekkoGameplayInputBridge`, `RunRollbackGekkoGameplayInputBridgeSelfTest`, `RollbackLabCase::GekkoGameplayInputSelfTest` | `GekkoAdvanceEvent` `inputs` buffer decoded as `uint32_t[player_count]` into `RollbackDecodedGameplayInput` | Added after the live-peer pipeline split HRG1 metadata from gameplay cache values. The decoder rejects null input buffers, unsupported player counts, mismatched byte lengths, negative frames, and invalid player-slot extraction. The self-test then composes HRG1 metadata drain with an explicit decoded gameplay input apply, proving the HRG1 payload hash remains envelope identity while the decoded `uint32_t` input is the only value allowed into the cache shadow. It also pins the deterministic decoded Gekko event-stream checksum `0x79B1B776`. Ghidra plate comment at `GetCachedInputForFrameInputLogSlot @ 0x1403F0720` records the `uint dwInputValue` basis; effective score is `100.0` with no fixable deductions. | Standalone `RollbackGekkoGameplayInputSelfTest` passed with `enabled=1`, `raw=1`, `raw_p0=1`, `raw_p1=1`, `null=1`, `bad_frame=1`, `bad_size=1`, `bad_players=1`, `bad_slot=1`, `pipeline=1`, `payload_separate=1`, `advance_decode=1`, `rollback_decode=1`, `no_desync=1`, `decoded_events=49`, `decoded_inputs=98`, `rollback_advances=33`, `checksum=0x79B1B776`, and `checksum_expected=1`. Full validation bundle `E:\myMods\reports\rollback_validation\rollback_validation_20260702-163631.json` reran the in-game request-file case; trace `replay_trace_20260702_163641_pid63692.jsonl` emitted `rollback_gekko_gameplay_input ok=true` with all strict gates true. Required strict replay regression from the same bundle `E:\myMods\reports\replay_tests\replay_seek_e2e_20260702-163852-seek.json`, trace `replay_trace_20260702_163853_pid11348.jsonl`, passed with `strict: PASS`, 4/4 watch cases, `2400/2400` observed frames, and 0 state mismatches. | Model/lab contract only. It decodes real Gekko advance-event buffers inside the deterministic Gekko stress session, but it is not yet wired to Steam/SC6 live peer packets or prediction writes in online matches. |
| End-to-end local rollback/Gekko pipeline | N/A | `Horse::RollbackEndToEndHarness`, `RunRollbackEndToEndSelfTest`, `RollbackLabCase::EndToEndSelfTest` | two local peers, decoded `uint32_t[2]` gameplay payload, HRG1 metadata envelope, online-session metadata prediction, cache prediction/confirmation, deterministic state checksum | Added after the readiness slice to prove the local layers compose in one gate. The harness decodes a two-player Gekko-style payload, seeds session-level remote metadata predictions on both peers, writes divergent gameplay predictions to each cache shadow, enqueues source/destination/session-bound HRG1 packets in both directions, proves stock-drain-incomplete metadata is left queued, drains metadata only after stock drain with correction required, proves HRG1 metadata/payload hash is not gameplay input, applies decoded confirmed inputs on the game thread, consumes confirmed cache entries by player/frame identity, rejects wrong peer/session identity, rejects network-thread cache writes, and compares predicted versus confirmed deterministic checksums. | Standalone `RollbackEndToEndSelfTest` in full validation `E:\myMods\reports\rollback_validation\rollback_validation_20260702-232726.json` passed with `decode=1`, `bridge=1`, `predict=1`, `predicted_diff=1`, `queued_only=1`, `stock_drain=1`, `metadata=1`, `correction=1`, `metadata_not_gameplay=1`, `confirm_apply=1`, `confirm_consume=1`, `state=1`, `wrong_identity=1`, `net_cache_reject=1`, `enqueued=2`, `drained=2`, `rejected=3`, `cache_writes=4`, and matching confirmed checksums. The request-file in-game lab `lab-end-to-end` also passed in the same bundle with trace `replay_trace_20260702_232935_pid67024.jsonl`. | Local composition proof only. It still does not replace Steam/SC6 live peer packet I/O, inject predictions in an online match, or prove `live_order_proven=true` with real peer traffic. |
| Local input delay guard | `ALuxBattleFrameInputLog+0x390` | `GetCachedInputForFrameInputLogSlot` | `nFrameIndexBase/InputDelay` | Ghidra notes observed builds leave this field zero. | Baseline build only. | Explicitly excluded from rollback part one. |
| Rollback lab shell | N/A | `Horse::RollbackLab`, `Horse::RollbackController` | Command-line/UI feature gate, request-file gate, JSONL trace probes, and live-online summary artifacts | Local implementation on 2026-07-01/02. Request-file gate atomically writes `Saved\rollback_lab_request.txt` with temp-file + flush/fsync + replace, consumes it when no explicit rollback command-line flags are present, retries transient read failures, and can consume a later request in the same SC6 process. This avoids Steam launch-parameter prompts and keeps live-only retries usable. `tools\rollback_lab_test_run.py` now uses that request file by default, reserving command-line rollback flags for `--force-command-line-args`, has `--cleanup-request-after` for attach-mode runs, parses exact `field=value` tokens for strict log gates, and has strict gates for the Gekko gameplay-input bridge, adapter gameplay decoder, live peer pipeline, end-to-end local pipeline, guarded live activation with direct readiness, route-provenance, and route-identity checks, stock-transport adapter provenance plus actual identity values, stock-observe receive-enqueue hook, and combined live-online-capture readiness. `tools\rollback_live_online_capture_analyze.py` independently analyzes the durable ReplayTrace JSONL artifact after capture, requires `case=live-online-capture` and the per-run `request_id`, and enforces the same readiness/live/sticky policy. `RollbackController` includes offline transport/session model validation surfaces, live-boundary observability, cache-injection, cache-prediction, Gekko adapter, Gekko gameplay-input bridge, live-transport queue, live-peer pipeline, end-to-end pipeline, guarded live activation, stock-transport surface, stock-transport observe, and live-online-capture probe cases; live-online verdicts wait for native image-base readiness before reporting boundary-hook readiness. | Full validation bundle `E:\myMods\reports\rollback_validation\rollback_validation_20260702-232726.json` passed after adapter-provenance and failure-token hardening: preflight SC6 close, build/deploy, `build-selftests`, 14 standalone self-tests including `RollbackEndToEndSelfTest` and `RollbackLiveActivationSelfTest`, 9 request-file in-game lab gates including `lab-end-to-end`, `lab-live-activation`, `stock-transport`, and the longer blank-start `live-online-capture` readiness gate, post-capture `analyze-live-online-capture-trace`, and strict replay. It wrote `E:\myMods\reports\rollback_validation\live_online_capture_readiness_20260702-232726.json` and `E:\myMods\reports\rollback_validation\live_online_capture_trace_readiness_20260702-232726.json`; analyzer input trace selected image-base-ready readiness with `stable_readiness_ok=true`, `stable_live_traffic_ok=false`, no boundary violation/regression, `require_case=live-online-capture`, and `require_request_id=20260702-232726`. No rollback launch parameters were used by the lab cases. | Developer-only lab and verification surface; not enabled in normal online matches. Cache-prediction requires an active replay/battle consumer, so the automated validation uses `replay_seek_test_run.py` rather than blank-start `rollback_lab_test_run.py`; real online capture additionally requires `--live-online-only` during an actual online peer/match. |
| Lifecycle epoch guard | `0x14470DE90`, `0x14470DE98` | `g_pLuxBattleCharaP1`, `g_pLuxBattleCharaP2`, `Horse::RollbackEpochMatchesLiveCharaPointers`, `Horse::ValidateRollbackLifecycleEpoch` | `LuxBattleCharaRollbackSlice *` globals, manifest epoch presence `0x03`, BattleManager/InputLog/chara/round/stage epoch fields | Ghidra MCP created `LuxBattleCharaRollbackSlice`, applied it to both live chara globals, and saved the program. Snapshot lifecycle validation now compares active presence, chara identity, BattleManager, InputLog, round number, round-start hash, and stage-context hash. | Final matrix waits until context is ready; every restore in `092631` passes the active-presence/chara epoch windows. `RollbackSnapshotSelfTest` boundary refusals passed count=9 for inactive snapshot/live epoch, missing chara epoch, changed BM, changed chara, changed InputLog, changed round number, changed round-start hash, and changed stage-context hash. | Same-process runtime restore refuses if captured chara pointers do not match live pointers or presence is not active. Snapshot byte restore helper refuses broader epoch mismatches before touching destination bytes; full live BM/InputLog/stage binding remains pending for online rollback. |
| Explicit snapshot copier | N/A | `Horse::CaptureRollbackSnapshotBytes`, `Horse::RestoreRollbackSnapshotBytes`, `Horse::RestoreRollbackSnapshotBytesIfEpochMatches` | Manifest rows marked `ExplicitSnapshot` plus lifecycle-validated byte restore helper | Implemented after Ghidra confirmed direct global ranges. Native HgCpuDirect is handled by `RollbackHgCpuSnapshot`, not by raw byte copying from the writer address. Low-context review found that the full validation runner could still execute a stale lower-case snapshot exe; remediation added the CMake target `RollbackSnapshotSelfTest` and changed the bundle to build/run `HorseMod\RollbackSnapshotSelfTest.exe`. | Full validation `E:\myMods\reports\rollback_validation\rollback_validation_20260703-012415.json` rebuilt/reran `RollbackSnapshotSelfTest.exe`, reporting `rollback snapshot self-test passed hash=0xA3B63486F0BE0E12 bytes=32` and `rollback snapshot boundary refusals passed count=9`. Final matrix requires `explicit_match=true` for all baseline/corrected windows. | Restore source for explicit global byte ranges only. Boundary-refusal helper proves mismatched lifecycle epochs leave destination bytes unchanged. Native HgCpuDirect, KHit topology, stage state, and InputLog cache have separate rows or remain pending. |
| Battle RNG state array | `0x14485EB30`, `0x14485EB94` | `g_adwLuxBattleLfsrState`, `g_dwLuxBattleLfsrIndex` | `byte[100]` plus `uint` index | Ghidra MCP `analyze_data_region`: state is `byte[100]`, index is `uint`; assembly compares index against `0x19` and writes dwords through the state array. | Final matrix and cache probes keep LFSR indices equal across baseline/predicted/corrected/restore phases. | `ExplicitSnapshot` global range. |
| Visual stage-wind RNG gate | `0x140331980`, `0x140333550`, `0x140332D60`, `0x140334750` | `IwWind_UpdateParallelOscillation`, `IwWind_UpdateRingInOscillation`, `IwWind_InitPlaneObject`, `IwWind_ScheduleNewWindEffect`, `Horse::RollbackScopedWindRngGate` | Presentation-only `LuxMoveVM_GetRandU32` callers gated during hidden resim | `RngTraceHook` proved warmed cache failures came from visual wind RNG calls; Ghidra plate comments were added and saved for the four wind functions. | Final cache and matrix traces have `wind_rng_gate_enabled=true`; rollback RNG caller phase totals remain zero in the hidden resim probes. | Hidden-resim side-effect gate. Broader visible-side-effect policy remains a Phase 8 online gate. |
| CMatrixBank control capsule | Chara `+0x35A0`, `+0x27760`; snapshot locals `+0x3590`, `+0x4DD0` | `Horse::RollbackCaptureMotionBankHistory`, `Horse::RollbackRestoreMotionBankHistory`, `Horse::CompareRollbackMotionBankHistories` | `0x38` control bytes per player/bank; physical provider buffers retained only as diagnostics | Ghidra and ReplayScrub evidence show the primary/secondary CMatrixBank control selects motion/bone provider buffers. The first low-context review found a false pass when full provider buffers differed; remediation now requires control match and separately reports provider-cache diagnostics. | Final cache window 30 `072103`, window 60 `072333`, combined `072952`, and matrix `072651` all have `*_motion_bank_control_match=true` and `*_motion_bank_mismatch_count=0`. | CMatrixBank control is restored and hashed. Physical provider/cache bytes are excluded from native byte policy as presentation cache with reason `motion-bank primary/secondary presentation cache; rollback restores control and MoveVM motion tail`. |
| MoveVM motion-tail capsule | Chara `+0x96490..+0x9748F` | `Horse::RollbackCaptureMoveVMMotionTail`, `Horse::RollbackRestoreMoveVMMotionTail` | Per-chara `0x1000` byte dynamic capsule | Ghidra MCP cleaned `LuxMoveVM_InitMotionPlayback @ 0x140300400`, typed/renamed parameters, and updated the plate comment. Decompile evidence links motion playback tail state to CMatrixBank provider/cache behavior. | Final cache traces `072103`, `072333`, `072952` and final matrix `072651` have `*_motion_tail_match=true`; matrix also has delayed-input divergence for windows `1,2,8,15,60`. | Same-lifecycle dynamic restore source. This is the gameplay-state companion to the provider-cache presentation exclusion. |
| Latest engine input globals | `0x144855700` | `g_LuxBattle_LatestEngineInput_PerPlayer` | `ulonglong[2]` | Ghidra xrefs show `LuxBattle_PerFrameTick` writes qword input and `LuxBattle_TickCharaInput` reads both qwords. Type retagged from `uint[2]` to `ulonglong[2]`. | Final delayed-input matrix forces P2 low-byte divergence and then requires corrected `explicit_match=true`. | `ExplicitSnapshot` global range plus direct step harness read/write of latest inputs. |
| Per-player input ring globals | `0x14485E750`, `0x14485EB20`, `0x14470DED0` | `g_LuxBattle_PerPlayerInputRing`, `g_LuxBattle_PerPlayerInputRingCursor`, `g_LuxBattle_InputRingBaseOffset_PerPlayer` | `FLuxBattleInputRing[2]`, cursor/base `int[2]` with alignment padding preserved | Ghidra MCP xrefs show 0x3D qword ring entries per player and base+cursor lookup in the chara input consumer. | Final matrix has `explicit_match=true` for every baseline and delayed correction window. | `ExplicitSnapshot` global ranges. |
| CPU command scheduler globals | `0x144715400` | `g_LuxBattle_CCpuCommandArray` | Two `0x60`-byte command slots, struct pending | Ghidra MCP xrefs show command helpers deriving slot address from `g_LuxBattle_CCpuCommandArray` with `index * 0x60`. Large struct promotion is deferred. | Final matrix has `explicit_match=true` for all windows. | `ExplicitSnapshot` global range with pending struct cleanup. |
| Native KHit node serializers | `0x14030EA10..0x14030EC40` | `KHit_WriteType`, `KHit_ReadType`, `KHit_WritePositionsAndRadius`, `KHit_ReadPositionsAndRadius`, `KHit_WriteTypeAndDoubleBuffer`, `KHit_ReadTypeAndDoubleBuffer`, `KHit_WriteTypeAndTriplePos`, `KHit_ReadTypeAndTriplePos` | `LuxSnapshotStream * __fastcall (KHitBase *pNode, LuxSnapshotStream *pSnapshotStream)` | Ghidra MCP set prototypes and plate comments for all eight functions; completeness reports effective `100.0`. Decompile proves native stream serializes node payloads, not list heads, next pointers, vtables, or authored tag/slot metadata. | Final matrix has `hgcpu_topology_match=true` and `hgcpu_unignored_mismatch_count=0` for all windows. | Rollback snapshots capture/restore KHit list-control bytes and reachable KHit node images before native HgCpu restore. |
| KHit topology capsule | Chara `+0x44470..+0x444BF`; heads `+0x44478`, `+0x44498`, `+0x444B8` | `Horse::RollbackCaptureKHitTopology`, `Horse::RollbackRestoreKHitTopology` | Per-player list-control bytes plus reachable `0xA0` KHit node images | Ghidra evidence above shows native HgCpu only walks live lists; `KHitWalker.hpp` documents list heads/counts and node roles. | Final matrix `072651`: every event has `hgcpu_topology_match=true`, `corrected_matches_baseline=true`, and `hgcpu_unignored_mismatch_count=0`. | Explicit dynamic restore source for topology not covered by native HgCpu. Restore refuses if chara pointers changed. |
| Dynamic HgCpu chara layout | P1 stream base `0`, P2 stream base `P1RecordBytes` | `Horse::RollbackHgCpuCharaRecordBytes` | `0x79AC + 0x41C + live_khit_node_bytes + 0x90` | ReplayScrub dynamic layout helper and Ghidra `WriteCharaStateToSnapshot` / `ReadCharaStateFromSnapshot` decomp show P2 starts after P1's variable KHit stream plus relocation records, not at a fixed `0x1400C`. | Baseline/delayed probes compare only native-used bytes and apply ignore ranges through the dynamic P2 base. | Fixes latent P2 offset policy bug; fixed `0x1400C` remains fallback only when topology is unavailable. |
| Baseline K-window resim | N/A | `RunRollbackResimWindowProbe(case=baseline-oracle)` | Capture S0, run confirmed frames, restore S0, rerun same frames | Direct stepping uses the `LuxBattle_PerFrameTick` bypass; HgCpu state includes native bytes plus KHit topology, CMatrixBank control, MoveVM motion tail, timer nodes, and explicit globals. | Final matrix `072651`: baseline windows `1,2,8,15,60` all have `ok=true`, `corrected_matches_baseline=true`, `explicit_match=true`, `hgcpu_policy_match=true`, `hgcpu_motion_bank_control_match=true`, `hgcpu_motion_tail_match=true`, and `hgcpu_unignored_mismatch_count=0`. | Same-input rollback equality test. |
| Delayed-input correction | N/A | `RunRollbackResimWindowProbe(case=delayed-input)` | Predict wrong P2 low input byte, restore S0, resim with confirmed input | Fault injection fails with `prediction-did-not-diverge` if the wrong prediction path is neutral. | Final matrix `072651`: delayed windows `1,2,8,15,60` all have `ok=true`, `predicted_differs_from_baseline=true`, `corrected_matches_baseline=true`, `hgcpu_policy_match=true`, and `hgcpu_unignored_mismatch_count=0`. | Rollback correction acceptance test across short, common, and max 60-frame windows. |
| Rollback resim matrix | N/A | `RollbackLabCase::ResimMatrix`, `tools\rollback_lab_test_run.py --require-resim-matrix` | Baseline-oracle and delayed-input cases for windows `1,2,8,15,60` | Matrix waits for lifecycle epoch binding before emitting per-window events and records wind-gate and HgCpu policy details. The Python harness now requires `hgcpu_policy=1`, `motion_bank_control=1`, `motion_tail=1`, and `unignored=0` in human logs as well as JSONL success. | Latest post-cache-prediction report `E:\myMods\reports\replay_tests\replay_seek_e2e_20260702-122054-seek.json`, trace `replay_trace_20260702_122057_pid85576.jsonl`: strict PASS, 10 matrix events, 0 failures, 5 delayed-input prediction-divergence cases, windows `1,2,8,15,60`, and no missing baseline/delayed cases. | Main rollback correctness matrix for the local same-process lab. |
| InputLog cache ownership trace | `BM+0x478`, `InputLog+0x394..+0x4418` | `Horse::RunRollbackInputLogOwnershipProbe`, `tools\replay_seek_test_run.py --rollback-lab-case cache-ownership --require-rollback-cache-ownership` | Full InputLog ownership window, `+0x3C0` cache, `+0x3B8/+0x3BC` mirror, `+0x3A4` master clock, `+0x4410` drain cursor | Runtime probe captures the InputLog window before delayed-input rollback resim, runs correction, captures after, and requires all identities/hashes/cursors to match. The replay harness directly asserts full/cache/drain equality, corrected resim equality, HgCpu policy, motion-bank control, motion tail, zero motion-bank mismatches, and zero unignored mismatches. | Latest post-cache-prediction report `E:\myMods\reports\replay_tests\replay_seek_e2e_20260702-121833-seek.json`, trace `replay_trace_20260702_121835_pid83588.jsonl`, has `rollback_cache_ownership ok=true`, strict analyzer PASS, 4/4 watch cases, `2400/2400` observed frames, and `0` state mismatches. | Current proof that direct local rollback resim does not mutate stock InputLog/cache state. Full online drain/read ordering remains a Phase 8 gate. |
| Strict replay seek regression | N/A | `tools\replay_seek_test_run.py --timeline-generation-mode lux-no-render --case-preset watch --watch-frames 600 --wait --analyze --strict ...` | Existing replay seek/watchback oracle | Required because rollback touched replay/tick/HgCpu/MoveVM-adjacent state, native online observe hooks, GekkoNet integration, WinSock UDP adapter code, and the live activation-candidate diagnostics linked into `HorseMod`. | Latest strict replay from full validation `E:\myMods\reports\rollback_validation\rollback_validation_20260703-023756.json` passed on the first attempt: final report `E:\myMods\reports\replay_tests\replay_seek_e2e_20260703-024010-seek.json`, trace `replay_trace_20260703_024010_pid24464.jsonl`, `strict: PASS`, 4/4 watch cases, `2400/2400` observed frames, 0 state mismatches, empty `failed_case_labels`, empty `failure_groups`, `final_passed=true`, `summary_passed=true`, `warm_process_retry=false`, `attempt_count=1`, `retry_count=0`, and max seek-validation elapsed `0.44s <= 0.50s`. | Regression proof that rollback lab changes did not break replay seek/watchback behavior. |
