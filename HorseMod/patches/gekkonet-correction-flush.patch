diff --git a/GekkoLib/include/gekkonet.h b/GekkoLib/include/gekkonet.h
index 9c55e12..fdeddea 100644
--- a/GekkoLib/include/gekkonet.h
+++ b/GekkoLib/include/gekkonet.h
@@ -195,6 +195,8 @@ GEKKONET_API void gekko_add_local_input(GekkoSession* session, int player, void
 
 GEKKONET_API GekkoGameEvent** gekko_update_session(GekkoSession* session, int* count);
 
+GEKKONET_API GekkoGameEvent** gekko_flush_corrections(GekkoSession* session, int* count);
+
 GEKKONET_API GekkoSessionEvent** gekko_session_events(GekkoSession* session, int* count);
 
 GEKKONET_API float gekko_frames_ahead(GekkoSession* session);
diff --git a/GekkoLib/include/private/session.h b/GekkoLib/include/private/session.h
index 38e16bd..4638094 100644
--- a/GekkoLib/include/private/session.h
+++ b/GekkoLib/include/private/session.h
@@ -20,6 +20,7 @@ struct GekkoSession {
     virtual i32 AddActor(GekkoPlayerType type, GekkoNetAddress* addr) = 0;
     virtual void AddLocalInput(i32 player, void* input) = 0;
     virtual GekkoGameEvent** UpdateSession(i32* count) = 0;
+    virtual GekkoGameEvent** FlushCorrections(i32* count) { *count = 0; return nullptr; }
     virtual GekkoSessionEvent** Events(i32* count) = 0;
     virtual f32 FramesAhead() = 0;
     virtual void NetworkStats(i32 player, GekkoNetworkStats* stats) = 0;
@@ -46,6 +47,8 @@ namespace Gekko {
 
         GekkoGameEvent** UpdateSession(i32* count) override;
 
+        GekkoGameEvent** FlushCorrections(i32* count) override;
+
         GekkoSessionEvent** Events(i32* count) override;
 
         f32 FramesAhead() override;
@@ -54,6 +57,8 @@ namespace Gekko {
         void NetworkPoll() override;
 
 	private:
+        GekkoGameEvent** UpdateSessionInternal(i32* count, bool advance);
+
 		void Poll();
 
 		bool AllActorsValid();
diff --git a/GekkoLib/src/game_session.cpp b/GekkoLib/src/game_session.cpp
index aef083a..5fedf63 100644
--- a/GekkoLib/src/game_session.cpp
+++ b/GekkoLib/src/game_session.cpp
@@ -119,6 +119,16 @@ void Gekko::GameSession::AddLocalInput(i32 player, void* input)
 
 GekkoGameEvent** Gekko::GameSession::UpdateSession(i32* count)
 {
+    return UpdateSessionInternal(count, true);
+}
+
+GekkoGameEvent** Gekko::GameSession::FlushCorrections(i32* count)
+{
+    return UpdateSessionInternal(count, false);
+}
+
+GekkoGameEvent** Gekko::GameSession::UpdateSessionInternal(i32* count, bool advance)
+{
     // reset session events
     _msg.session_events.Reset();
 
@@ -151,7 +161,7 @@ GekkoGameEvent** Gekko::GameSession::UpdateSession(i32* count)
         SessionIntegrityCheck();
 
         // then advance the session
-        if (_game_events.AddAdvanceEvent(_sync, false, _runahead_frames > 0)) {
+        if (advance && _game_events.AddAdvanceEvent(_sync, false, _runahead_frames > 0)) {
             if (!_config.limited_saving) {
                 _game_events.AddSaveEvent(_sync, _storage, &_last_saved_frame);
             }
@@ -160,7 +170,7 @@ GekkoGameEvent** Gekko::GameSession::UpdateSession(i32* count)
         }
 
         // run ahead if configured
-        HandleRunahead();
+        if (advance) HandleRunahead();
     }
 
     *count = _game_events.Count();
diff --git a/GekkoLib/src/gekkonet.cpp b/GekkoLib/src/gekkonet.cpp
index 87a5bc5..0f8cb30 100644
--- a/GekkoLib/src/gekkonet.cpp
+++ b/GekkoLib/src/gekkonet.cpp
@@ -74,6 +74,11 @@ GekkoGameEvent** gekko_update_session(GekkoSession* session, int* count)
     return session->UpdateSession(count);
 }
 
+GekkoGameEvent** gekko_flush_corrections(GekkoSession* session, int* count)
+{
+    return session->FlushCorrections(count);
+}
+
 GekkoSessionEvent** gekko_session_events(GekkoSession* session, int* count)
 {
     return session->Events(count);
